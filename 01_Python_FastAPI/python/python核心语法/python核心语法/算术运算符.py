# 算是运算符的语法
a=10
b=3

sum_res=a+b
diff_res=a-b
mul_res=a*b
div_res=a/b
floor_res=a//b


print(sum_res)
print(diff_res)
print(mul_res)
print(div_res)
print(floor_res)

# 注意看python这里有两个除法，一个是保留小数的，另外一个则是向下取整，和cpp里面的a/b一样

# 而且值得注意的一点就是python的加减乘除没有数值限制
# 只要你的内存够用，它就能给你算出来
# 这一点比cpp,java好，不用高精度